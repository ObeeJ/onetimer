services:
  - type: web
    name: onetimer-app
    runtime: docker
    dockerfilePath: ./Dockerfile.multi
    plan: starter
    region: oregon
    branch: main
    healthCheckPath: /api/healthz
    envVars:
      # Core configuration
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000

